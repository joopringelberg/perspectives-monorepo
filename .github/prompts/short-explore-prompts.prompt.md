Use the Explore agent, quick: inspect packages/perspectives-core, trace where transaction recipients are selected, and return the likely fix point plus the nearest validating test.

Use the Explore agent, quick: find the code path that sends transactions and tell me where recipients are selected.

Use the Explore agent, medium: inspect perspectives-core and summarize how perspective serialization works, with likely files and functions involved.

Use the Explore agent, thorough: compare all references to "serialization user", explain which ones are real vs fictive/internal, and identify the safest place to block outgoing transactions to that user.