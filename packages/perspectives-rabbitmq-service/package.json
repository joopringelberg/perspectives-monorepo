{
  "name": "perspectives-rabbitmq-service",
  "version": "1.3.0",
  "description": "A simple service that proxies a request for registration to a local RabbitMQ server. It should itself be a back-end service that a solid HTTP server such as Apache proxies to.",
  "main": "src/selfregister.js",
  "scripts": {
    "start": "node src/selfregister.js --config=./config.json",
    "start:example": "bash startService.example.sh",
    "test": "echo \"No tests configured\" && exit 0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/joopringelberg/perspectives-monorepo.git"
  },
  "keywords": [
    "perspectives",
    "rabbitmq",
    "amqp"
  ],
  "author": "Joop Ringelberg",
  "license": "GPL-3.0-or-later",
  "dependencies": {
    "winston": "^3.17.0",
    "yargs": "^17.7.2"
  }
}
